{% extends 'base.html.twig' %}

{% block title %}EcosystemPartners{% endblock %}

{% block body %}
    <h1>EcosystemPartners</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ecosystem_partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ecosystem_partner.name }}</td>
            </tr>
            <tr>
                <th>Partner_count</th>
                <td>{{ ecosystem_partner.partnerCount }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ecosystem_partner.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ecosystem_partners_index') }}">back to list</a>

    <a href="{{ path('app_ecosystem_partners_edit', {'id': ecosystem_partner.id}) }}">edit</a>

    {{ include('ecosystem_partners/_delete_form.html.twig') }}
{% endblock %}
