<form method="post" action="{{ path('app_procurement_bids_delete', {'id': procurement_bid.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ procurement_bid.id) }}">
    <button class="btn">Delete</button>
</form>
