{% extends 'base.html.twig' %}

{% block title %}ResearchPriorities{% endblock %}

{% block body %}
    <h1>ResearchPriorities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ research_priority.id }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ research_priority.priority }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_research_priorities_index') }}">back to list</a>

    <a href="{{ path('app_research_priorities_edit', {'id': research_priority.id}) }}">edit</a>

    {{ include('research_priorities/_delete_form.html.twig') }}
{% endblock %}
