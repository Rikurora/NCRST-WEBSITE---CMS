{% extends 'base.html.twig' %}

{% block title %}ResourceCategories{% endblock %}

{% block body %}
    <h1>ResourceCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ resource_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resource_categories_index') }}">back to list</a>

    <a href="{{ path('app_resource_categories_edit', {'id': resource_category.id}) }}">edit</a>

    {{ include('resource_categories/_delete_form.html.twig') }}
{% endblock %}
