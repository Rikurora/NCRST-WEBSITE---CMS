<form method="post" action="{{ path('app_iks_resources_delete', {'id': iks_resource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ iks_resource.id) }}">
    <button class="btn">Delete</button>
</form>
