<form method="post" action="{{ path('app_board_commissioners_delete', {'id': board_commissioner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ board_commissioner.id) }}">
    <button class="btn">Delete</button>
</form>
