<form method="post" action="{{ path('app_infrastructure_utilization_delete', {'id': infrastructure_utilization.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infrastructure_utilization.id) }}">
    <button class="btn">Delete</button>
</form>
