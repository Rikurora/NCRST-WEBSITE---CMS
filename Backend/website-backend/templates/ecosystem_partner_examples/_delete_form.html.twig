<form method="post" action="{{ path('app_ecosystem_partner_examples_delete', {'id': ecosystem_partner_example.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ecosystem_partner_example.id) }}">
    <button class="btn">Delete</button>
</form>
