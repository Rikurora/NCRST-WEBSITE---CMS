{% extends 'base.html.twig' %}

{% block title %}InnovationChallengeCategories index{% endblock %}

{% block body %}
    <h1>InnovationChallengeCategories index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Category</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for innovation_challenge_category in innovation_challenge_categories %}
            <tr>
                <td>{{ innovation_challenge_category.id }}</td>
                <td>{{ innovation_challenge_category.category }}</td>
                <td>
                    <a href="{{ path('app_innovation_challenge_categories_show', {'id': innovation_challenge_category.id}) }}">show</a>
                    <a href="{{ path('app_innovation_challenge_categories_edit', {'id': innovation_challenge_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_innovation_challenge_categories_new') }}">Create new</a>
{% endblock %}
