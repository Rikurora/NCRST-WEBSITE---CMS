<form method="post" action="{{ path('app_ai_initiatives_delete', {'id': ai_initiative.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ai_initiative.id) }}">
    <button class="btn">Delete</button>
</form>
