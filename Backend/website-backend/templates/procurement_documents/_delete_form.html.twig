<form method="post" action="{{ path('app_procurement_documents_delete', {'id': procurement_document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ procurement_document.id) }}">
    <button class="btn">Delete</button>
</form>
