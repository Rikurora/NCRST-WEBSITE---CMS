{% extends 'base.html.twig' %}

{% block title %}VacancyRequirements{% endblock %}

{% block body %}
    <h1>VacancyRequirements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vacancy_requirement.id }}</td>
            </tr>
            <tr>
                <th>Requirement</th>
                <td>{{ vacancy_requirement.requirement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vacancy_requirements_index') }}">back to list</a>

    <a href="{{ path('app_vacancy_requirements_edit', {'id': vacancy_requirement.id}) }}">edit</a>

    {{ include('vacancy_requirements/_delete_form.html.twig') }}
{% endblock %}
