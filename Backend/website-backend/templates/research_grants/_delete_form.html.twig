<form method="post" action="{{ path('app_research_grants_delete', {'id': research_grant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ research_grant.id) }}">
    <button class="btn">Delete</button>
</form>
