<form method="post" action="{{ path('app_news_articles_delete', {'id': news_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_article.id) }}">
    <button class="btn">Delete</button>
</form>
