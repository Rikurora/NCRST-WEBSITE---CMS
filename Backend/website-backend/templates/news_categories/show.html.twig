{% extends 'base.html.twig' %}

{% block title %}NewsCategories{% endblock %}

{% block body %}
    <h1>NewsCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ news_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ news_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_news_categories_index') }}">back to list</a>

    <a href="{{ path('app_news_categories_edit', {'id': news_category.id}) }}">edit</a>

    {{ include('news_categories/_delete_form.html.twig') }}
{% endblock %}
