<form method="post" action="{{ path('app_iks_iniative_outcomes_delete', {'id': iks_iniative_outcome.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ iks_iniative_outcome.id) }}">
    <button class="btn">Delete</button>
</form>
