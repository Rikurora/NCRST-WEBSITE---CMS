<form method="post" action="{{ path('app_impact_metrics_delete', {'id': impact_metric.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ impact_metric.id) }}">
    <button class="btn">Delete</button>
</form>
