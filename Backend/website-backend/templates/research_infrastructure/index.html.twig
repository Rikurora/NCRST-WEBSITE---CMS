{% extends 'base.html.twig' %}

{% block title %}ResearchInfrastructure index{% endblock %}

{% block body %}
    <h1>ResearchInfrastructure index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for research_infrastructure in research_infrastructures %}
            <tr>
                <td>{{ research_infrastructure.id }}</td>
                <td>
                    <a href="{{ path('app_research_infrastructure_show', {'id': research_infrastructure.id}) }}">show</a>
                    <a href="{{ path('app_research_infrastructure_edit', {'id': research_infrastructure.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_research_infrastructure_new') }}">Create new</a>
{% endblock %}
