{% extends 'base.html.twig' %}

{% block title %}ResearchInfrastructure{% endblock %}

{% block body %}
    <h1>ResearchInfrastructure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ research_infrastructure.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_research_infrastructure_index') }}">back to list</a>

    <a href="{{ path('app_research_infrastructure_edit', {'id': research_infrastructure.id}) }}">edit</a>

    {{ include('research_infrastructure/_delete_form.html.twig') }}
{% endblock %}
