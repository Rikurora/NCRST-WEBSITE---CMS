<form method="post" action="{{ path('app_research_infrastructure_delete', {'id': research_infrastructure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ research_infrastructure.id) }}">
    <button class="btn">Delete</button>
</form>
