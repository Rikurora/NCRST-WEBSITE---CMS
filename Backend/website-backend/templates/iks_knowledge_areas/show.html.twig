{% extends 'base.html.twig' %}

{% block title %}IksKnowledgeAreas{% endblock %}

{% block body %}
    <h1>IksKnowledgeAreas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ iks_knowledge_area.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ iks_knowledge_area.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ iks_knowledge_area.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ iks_knowledge_area.icon }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ iks_knowledge_area.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iks_knowledge_areas_index') }}">back to list</a>

    <a href="{{ path('app_iks_knowledge_areas_edit', {'id': iks_knowledge_area.id}) }}">edit</a>

    {{ include('iks_knowledge_areas/_delete_form.html.twig') }}
{% endblock %}
