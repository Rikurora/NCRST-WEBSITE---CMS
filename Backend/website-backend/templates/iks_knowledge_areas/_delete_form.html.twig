<form method="post" action="{{ path('app_iks_knowledge_areas_delete', {'id': iks_knowledge_area.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ iks_knowledge_area.id) }}">
    <button class="btn">Delete</button>
</form>
