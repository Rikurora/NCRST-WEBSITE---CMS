{% extends 'base.html.twig' %}

{% block title %}IksKnowledgeAreas index{% endblock %}

{% block body %}
    <h1>IksKnowledgeAreas index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Icon</th>
                <th>Color</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for iks_knowledge_area in iks_knowledge_areas %}
            <tr>
                <td>{{ iks_knowledge_area.id }}</td>
                <td>{{ iks_knowledge_area.title }}</td>
                <td>{{ iks_knowledge_area.description }}</td>
                <td>{{ iks_knowledge_area.icon }}</td>
                <td>{{ iks_knowledge_area.color }}</td>
                <td>
                    <a href="{{ path('app_iks_knowledge_areas_show', {'id': iks_knowledge_area.id}) }}">show</a>
                    <a href="{{ path('app_iks_knowledge_areas_edit', {'id': iks_knowledge_area.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_iks_knowledge_areas_new') }}">Create new</a>
{% endblock %}
