{% extends 'base.html.twig' %}

{% block title %}InternshipDepartments{% endblock %}

{% block body %}
    <h1>InternshipDepartments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internship_department.id }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ internship_department.department }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_internship_departments_index') }}">back to list</a>

    <a href="{{ path('app_internship_departments_edit', {'id': internship_department.id}) }}">edit</a>

    {{ include('internship_departments/_delete_form.html.twig') }}
{% endblock %}
