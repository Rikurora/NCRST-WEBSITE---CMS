<form method="post" action="{{ path('app_internship_departments_delete', {'id': internship_department.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ internship_department.id) }}">
    <button class="btn">Delete</button>
</form>
